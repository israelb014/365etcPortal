{
  "name": "@renewals/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply renewals --local",
    "migrate:remote": "wrangler d1 migrations apply renewals --remote",
    "deploy": "wrangler d1 migrations apply renewals --remote && wrangler deploy"
  },
  "dependencies": {
    "@renewals/shared": "1.0.0",
    "hono": "4.13.9",
    "jose": "6.2.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260925.1",
    "wrangler": "4.140.0"
  }
}
